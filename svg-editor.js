(function(jQuery) {
    if (typeof(jQuery.reshu) === "undefined") {
        jQuery.reshu = {};
    }

    var plugin;

    plugin = jQuery.reshu.svgeditor = function(svg) {
	console.log(svg)
    }

})(jQuery);
